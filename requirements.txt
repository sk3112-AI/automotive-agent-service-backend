fastapi
uvicorn
pandas
openai
supabase
python-dotenv
google-cloud-aiplatform
# Markdown‑to‑HTML converter used by suggest_offer_llm
markdown-it-py
# tzdata # No longer explicitly needed here if installed via apt-get in Dockerfile, but harmless if left.
slack_sdk==3.33.4
python-multipart>=0.0.9
